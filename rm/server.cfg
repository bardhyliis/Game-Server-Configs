# Minimal RedM server.cfg

# Endpoints
endpoint_add_tcp "0.0.0.0:30120"
endpoint_add_udp "0.0.0.0:30120"

# Server info
sv_hostname "SERVERNAME"
sv_maxclients 32

# Core resources
ensure mapmanager
ensure chat
ensure sessionmanager
ensure hardcap
ensure rconlog
